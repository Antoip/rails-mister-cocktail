<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, class: "select2" %>
  <%= f.submit "Add in shaker", class: "btn btn-primary w-100" %>
<% end %>
